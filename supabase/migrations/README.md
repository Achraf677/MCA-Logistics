# Migrations

Le dossier ne contient volontairement plus de fichiers de migration : le schéma de référence est
`supabase/schema.sql` (dump complet). Les changements de schéma sont appliqués via l'outil Supabase.

Anciennes migrations mal datées : voir `../migrations_legacy/`.
